# Generate a unique value for every deployment; never commit the real value.
DJANGO_SECRET_KEY=replace-with-a-unique-random-value

# Keep disabled in production.
DJANGO_DEBUG=false

# Comma-separated hostnames accepted by Django.
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
